fastapi==0.116.1
uvicorn==0.35.0
email-validator==2.2.0
# Auth uses Python stdlib (hashlib/hmac/base64) — no extra crypto deps needed.
# To upgrade to production-grade bcrypt: pip install python-jose[cryptography] passlib[bcrypt]
